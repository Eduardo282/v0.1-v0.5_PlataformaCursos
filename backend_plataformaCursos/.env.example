# ========================================
# CONFIGURACIÓN DEL BACKEND
# ========================================

# Puerto del servidor
PORT=8000

# URL del frontend (para enlaces en emails)
FRONTEND_URL=http://localhost:5173

# ========================================
# CONFIGURACIÓN DE EMAIL (RESEND)
# ========================================

# API Key de Resend (GRATIS - 3,000 emails/mes)
# Para obtenerla:
# 1. Ve a: https://resend.com/signup
# 2. Regístrate con tu email (SIN tarjeta de crédito)
# 3. Ve a Dashboard → API Keys
# 4. Click "Create API Key"
# 5. Copia la key que empieza con "re_"
RESEND_API_KEY=re_tu_api_key_aqui

# Email remitente (usa onboarding@resend.dev si no tienes dominio propio)
FROM_EMAIL=onboarding@resend.dev

# ========================================
# CONFIGURACIÓN DE JWT
# ========================================

# Clave secreta para firmar tokens JWT
# IMPORTANTE: Cambiar en producción por una clave segura
# Puedes generar una con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=tu_clave_super_secreta_cambiar_en_produccion

# ========================================
# CONFIGURACIÓN DE BASE DE DATOS MYSQL
# ========================================

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=tu_password
DB_NAME=plataformacursos
DB_PORT=3306

# ========================================
# INSTRUCCIONES
# ========================================

# 1. Copia este archivo y renómbralo a ".env" (sin el .example)
# 2. Completa todos los valores
# 3. NO subas el archivo .env a Git (ya está en .gitignore)
# 4. Reinicia el servidor después de cambiar variables
